VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_DATABASE_URL=
VITE_FIREBASE_PROJECT_ID=
VITE_PAYSTACK_PUBLIC_KEY=
# WhatsApp ordering (Cart page). Digits only, international format without +,
# e.g. 27821234567. Leave blank to disable the WhatsApp order CTA.
VITE_WHATSAPP_NUMBER=
# Admin access is controlled server-side via the admin_users table + is_admin()
# RLS helper (see supabase/migrations/*_admin_authorization.sql). No client-side
# admin env var is used. Add/remove admins by editing the admin_users table.
